apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-petclinic.fullname" . }}-test-connection"
  labels:
    {{- include "my-petclinic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-petclinic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
